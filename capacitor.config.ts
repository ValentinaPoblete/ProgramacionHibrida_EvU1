import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'u1eval.figuras.geometricas',
  appName: 'u1eval-figuras-geometricas',
  webDir: 'www'
};

export default config;
